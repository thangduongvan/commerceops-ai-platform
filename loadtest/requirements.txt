# V2 (Horizontal Scaling) load-testing tool. Kept separate from the app's own
# requirements.txt so the production image never ships a load-test dependency.
locust>=2.29,<3.0

# V4 (Asynchronous Processing): queue_experiment.py talks to SQS/LocalStack
# directly (not via the app's HTTP API), so it needs boto3 too.
boto3>=1.34,<2.0

# V5 (Reliability): chaos_experiment.py drives the app's HTTP API and the
# stand-in gateway's /admin/chaos endpoint.
# V6 (Database HA): ha_experiment.py uses the same HTTP client for lag /
# failover drills (psql goes through `docker compose exec`, AWS via the CLI).
httpx>=0.27,<1.0
